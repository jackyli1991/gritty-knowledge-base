
## n

node 版本管理工具。

```Zsh
brew install n

n ls # list all
n install <version>
n <version> # switch node
n rm <version> # remove node
n latest/current # 安装最新版本
n engine # 安装 package.json 对应 node 版本
n run <version> <command> # 使用指定版本node执行命令
```

